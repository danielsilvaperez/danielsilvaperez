name: CI

on:
  push:
  pull_request:

jobs:
  profile-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ensure profile README exists
        run: test -f README.md
